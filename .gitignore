# dependencies
node_modules/

# next.js
.next/
out/

# env
.env
.env.local
.env.*.local

# uploads (FDA docs, etc.)
uploads/

# logs & OS
*.log
.DS_Store
Thumbs.db

# IDE / editor
.idea/
.vscode/
.cursor/
*.swp

# test / build
coverage/
dist/
.turbo/
*.tsbuildinfo

# misc
next-env.d.ts

# AI agent context (local only, not shared via git)
CLAUDE.md

# Local migration workbooks and run state (see sales migration/README.md, deliveries migrations/README.md)
sales migration/*.xlsx
sales migration/*.json
deliveries migrations/*.xlsx
deliveries migrations/*.json
returned migration/*.xlsx
returned migration/*.json
payouts migration/*.xlsx
payouts migration/*.json
discrepancies fix/*.xlsx
discrepancies fix/*.json
# Regenerated by summarize-discrepancies-for-admin.ts (keep README.md only)
discrepancies fix/ADMIN-ATTENTION.md

# credentials / keys (never commit)
credentials.json
*-credentials.json
*.pem
.vercel/
